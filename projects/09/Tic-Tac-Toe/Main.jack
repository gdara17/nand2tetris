/** Initializes a new Tic-Tac-Toe game and starts running it. */
class Main {
    function void main() {
        var TicTacToeGame game;
        let game = TicTacToeGame.new(3);
        do game.run();
        do game.dispose();
        return;
    }
}